language: java
jdk:
  - oraclejdk8
script:
  - mvn test jacoco:report
  - mvn sonar:sonar -Dsonar.projectKey=basic-utils -Dsonar.organization=schinzel-github -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=$SONAR_TOKEN
notifications:
  email: false
  